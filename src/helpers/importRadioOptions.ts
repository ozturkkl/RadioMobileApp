// @ts-nocheck
import {RADIO_ICON_SRC, RADIO_NAME, BACKGROUND_SRC} from 'react-native-dotenv'

export default {
    RADIO_ICON: require(RADIO_ICON_SRC),
    RADIO_NAME: RADIO_NAME,
    BACKGROUND_IMG: require(BACKGROUND_SRC)
};